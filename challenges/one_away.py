# Suppose there are 3 types of edits that can be performed on a string (insert, remove, replace). Given two strings, write a function to check if they are one edit (or zero edits) away.

def task(string_a: str, string_b: str):
	return True

